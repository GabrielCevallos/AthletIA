Buenísima pregunta, porque acá ya no es *qué hacer*, sino **cómo pedírselo bien al agente** para que no haga un desastre 😄
La clave es darle **instrucciones arquitectónicas**, no “traduce esto”.

Te dejo **un prompt base** y luego te explico **por qué funciona** y **qué vigilar**.

---

## 1️⃣ Prompt recomendado para tu agente (copiar / pegar)

> Tengo una app web frontend en React (Vite) que actualmente está **solo en español**.
> Quiero implementar **internacionalización (i18n)** con soporte inicial para **español e inglés**, con cambio de idioma en tiempo real.
>
> Requisitos de arquitectura:
>
> * No debe haber textos hardcodeados visibles en los componentes.
> * Todos los textos deben reemplazarse por **claves semánticas**.
> * Las traducciones deben organizarse por **dominio funcional**: usuarios, rutinas, splits, ejercicios y comunes.
> * Los componentes React **no deben contener lógica de idioma**, solo consumir traducciones.
> * El idioma debe ser un **estado global** de la aplicación.
> * El sistema debe permitir agregar más idiomas sin modificar componentes existentes.
>
> Objetivo:
>
> * Mantener el comportamiento actual de la app.
> * Migrar progresivamente desde español hardcodeado a un sistema multidioma.
>
> Procede módulo por módulo:
>
> 1. Crear la estructura de traducciones.
> 2. Reemplazar textos visibles por claves.
> 3. Asegurar que la UI sigue funcionando igual.
>
> No refactorices lógica de negocio ni estilos, solo textos e infraestructura de i18n.

---

## 2️⃣ Cómo pensar la migración (para que el agente no se pierda)

Tu app **ya tiene una estructura excelente**, úsala como mapa mental.

Arquitectura conceptual:

```
Traducciones
 ├─ usuarios
 ├─ rutinas
 ├─ splits
 ├─ ejercicios
 └─ common
```

Cada módulo:

* “posee” sus textos
* no conoce otros módulos
* no conoce idiomas

Esto es **oro puro** para i18n.

---

## 3️⃣ Cómo pedirle que convierta un módulo (ejemplo mental)

En vez de:

> “Traduce todo a inglés”

le estás diciendo implícitamente:

> “Extrae significados y dales identidad”

El agente debe:

1. Identificar textos visibles del módulo
2. Asignarles claves **estables**
3. Mover el texto actual (español) al diccionario
4. Sustituir el texto por una referencia

Importante:
👉 **El español pasa a ser solo un idioma más**, no el “idioma base del código”.

---

## 4️⃣ Claves: cómo deben ser (esto es clave que se lo digas)

Dile explícitamente al agente:

* No usar textos como claves
  ❌ `"Iniciar sesión"`
  ✅ `"auth.login.button"`

* Las claves reflejan intención, no redacción
  ❌ `"users.createUserTitle"`
  ✅ `"users.create.title"`

* Mantener las claves aunque el texto cambie

---

## 5️⃣ Estrategia segura: migración progresiva

Muy importante indicarle esto:

> No migres toda la app de una sola vez.
> Empieza por un módulo completo (por ejemplo `usuarios`) y valida el patrón antes de continuar.

Arquitecturalmente:

* Puedes tener **partes traducidas** y **partes hardcodeadas** temporalmente
* Eso es normal
* No rompe la app

---

## 6️⃣ Qué revisar tú como humano (checklist)

Cuando el agente termine un módulo, revisa:

✅ No hay strings visibles en JSX
✅ Las claves tienen sentido semántico
✅ El módulo no “sabe” qué idioma está activo
✅ El español sigue viéndose igual
✅ Agregar inglés no toca componentes

Si falla uno de estos, corrige **antes** de seguir.

---

## 7️⃣ Qué NO pedirle (esto rompe todo)

❌ “Detecta idioma y renderiza según eso”
❌ “Usa if (lang === 'es')”
❌ “Duplica componentes por idioma”
❌ “Traduce los textos dentro del JSX”

---

## 8️⃣ Frase mágica para cerrar el prompt (muy útil)

Añade esto al final:

> Si en algún punto hay una decisión ambigua, prioriza **arquitectura limpia y escalable** sobre rapidez.

Los agentes suelen comportarse *mucho mejor* con esa frase.

---

Si quieres, en el próximo mensaje puedo:

* ayudarte a **definir las claves** de uno de tus módulos (usuarios, rutinas, etc.)
* revisar un prompt que ya estés usando
* o darte un **orden óptimo** de migración según riesgo

Tú dime 💡
